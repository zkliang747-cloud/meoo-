---
name: hero-fullscreen
version: 1.0.0
description: Fullscreen Hero design style for web interfaces
keywords: [全屏, 英雄区, 大图, 视频, 品牌]
author: StyleKit
style_type: layout
---

# Fullscreen Hero Style

## Overview
以全屏大图或视频为背景的英雄区布局，通过震撼的视觉效果抓住注意力，适合品牌展示、产品发布、活动宣传。

## Philosophy
Fullscreen Hero（全屏英雄区布局）是一种以视觉冲击力为核心的布局方式，用全屏图片或视频创造沉浸式第一印象。

## Design Tokens

### Border
- Width: `border-2`
- Color: `border-white`
- Radius: `rounded-full`

### Shadow
- Small: `shadow-none`
- Medium: `shadow-none`
- Large: `shadow-none`
- Hover: `shadow-none`

### Typography
- Heading: `font-bold leading-tight`
- Body: `font-sans`
- Hero: `text-5xl md:text-7xl lg:text-8xl`
- H1: `text-4xl md:text-6xl`
- Body: `text-lg md:text-xl lg:text-2xl`

### Spacing
- Section: `py-0`
- Container: `px-4 md:px-6 lg:px-8`
- Card: `p-8 md:p-10`

### Colors
- Primary BG: `bg-black`
- Secondary BG: `bg-black/50`
- Primary Text: `text-white`
- Primary Button: `bg-white text-black`

## Component Recipes

### Button
- Base: `font-semibold rounded-full transition-all duration-200`
- Primary: `bg-white text-black`
- Secondary: `bg-transparent text-white border-2 border-white`
- Outline: `bg-transparent text-white/80 border border-white/40`

- Hover: `hover:bg-white/90 hover:shadow-lg`

### Card
- Base: `bg-white/10 backdrop-blur-sm rounded-2xl border border-white/20 text-white transition-all duration-200`
- Default: ``
- Solid: `bg-white text-zinc-900 border-white/0 shadow-xl`
- Dark: `bg-black/40 border-white/10`

- Hover: `hover:bg-white/15 hover:border-white/30`

### Input
- Base: `w-full bg-white/10 backdrop-blur-sm border border-white/30 rounded-full text-white placeholder:text-white/60 focus:outline-none transition-all duration-200`
- Default: ``
- Solid: `bg-white text-zinc-900 placeholder:text-zinc-400 border-white/0`

## Forbidden Patterns
These classes must NEVER be used in Fullscreen Hero style:

- `shadow-sm`
- `shadow-md`
- `shadow-lg`
- `shadow-xl`
- `bg-zinc-50`
- `bg-gray-50`
- `bg-white`
- `text-zinc-900`
- `text-gray-900`
- `text-black`

- Pattern: `^shadow-(?!none)`
- Pattern: `^bg-(?:zinc|gray)-(?:50|100)`
- Pattern: `^border-(?:zinc|gray)-`

### Why:
- `shadow-lg`: Hero Fullscreen uses overlays and contrast instead of shadows
- `bg-white`: Hero Fullscreen uses dark backgrounds with image/video overlays
- `text-black`: Hero Fullscreen uses white text on dark overlaid backgrounds

## Do's
- 使用 h-screen 或 min-h-screen 确保全屏
- 图片使用 object-cover 保持比例填充
- 添加渐变或半透明遮罩保证文字可读
- 内容绝对定位或 flex 居中
- 提供滚动提示引导用户往下看
- 视频背景静音自动播放
- 提供降级方案（图片替代视频）
- 主 CTA 按钮 hover:-translate-y-1 + hover:shadow-[0_8px_28px_rgba(0,0,0,0.5)]（重力浮起感）
- 特性卡片使用 group 类 + hover:-translate-y-2 + hover:shadow-[0_16px_40px_rgba(0,0,0,0.5)]
- 图标容器 group-hover:scale-110 微交互
- 所有按钮 active:scale-[0.98] active:translate-y-0 + focus:ring-2 focus:ring-white/80 focus:ring-offset-2

## Don'ts
- 禁止文字直接放在复杂背景上无遮罩
- 禁止使用低质量或拉伸的图片
- 禁止忽略移动端的适配
- 禁止内容占据全部空间无留白
- 禁止视频有声音自动播放
- 禁止按钮缺少 active:scale-[0.98]（全屏背景下按钮无触觉确认感极差）
- 禁止卡片无 group 类（图标 group-hover:scale-110 微交互依赖 group）

## Layout Archetypes
- `landing-hero-centered`: Centered headline, subtitle, and CTA. Clean and focused.
- `landing-hero-split`: Text on left, image/visual on right. Great for product showcases.
- `landing-hero-fullscreen`: Full viewport hero with background image or color. High impact.
- `landing-video-hero`: Full-screen video background with overlay text. High impact for product demos.
- `landing-saas-pricing`: Complete SaaS pricing page with hero, pricing tiers, FAQ, and CTA.
- `landing-waitlist`: Minimal pre-launch page with email collection and countdown.

## Example Usage

```
Generate a landing page with:
- Style: hero-fullscreen
- Archetype: landing-hero-centered
- Sections: hero, features, cta, footer
- Components: heading (hero variant), button (primary, lg), card (default)
```

### Example Prompts:
- "产品发布页"
- "品牌故事"
- "活动宣传"

---
Generated by StyleKit · https://stylekit.dev
Style: 全屏英雄区布局 (Fullscreen Hero)